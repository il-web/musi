# Normalize to LF in the repo and on checkout — keeps shell scripts valid on the
# Linux Pi and avoids CRLF churn from the Windows dev machine.
* text=auto eol=lf
*.png  binary
*.jpg  binary
*.jpeg binary
*.ico  binary
*.ttf  binary
*.otf  binary
*.scad text eol=lf
