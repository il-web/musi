.venv/
node_modules/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/
*.db
*.db-wal
*.db-shm
art_cache/
music/
.env

# OTA signing public key — safe to publish, but deliberately NOT committed:
# it must reach the Pi out-of-band (scp), not through the repo it protects.
# Re-export any time: gpg --armor --export 52BA31D3E4F34050
musi-signing.pub

# local dev artifacts (machine-specific)
dev.env
dev_art/
.superpowers/
.ui-capture/
.vscode/
out/

# MPD runtime files
mpd/state
mpd/log
mpd/database
mpd/sticker.sql
mpd/pid
mpd/playlists/
mpd/

# local-only (kept on dev machine, not published)
3D model/
docs/superpowers/
pcb/
